# Cadence browser values are derived from the canonical runtime configuration.
#
# Select an approved config with the API generator, for example:
#   npm run config:ci
#
# Do not maintain VITE_* values here or place server credentials in browser config.
