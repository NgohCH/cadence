# VS003 — Durable Discussion Read and Return Path

**Status:** Implementation and evidence complete; closure reconciliation candidate
**Version:** 1.0
**Milestone:** M1 — Controlled Pilot
**Predecessors:** VS-001 Complete; VS-002 Complete

## Objective

Provide the smallest user-facing capability required for a controlled pilot to
recover shared Discussion context after entering, reloading, leaving, or
returning to a configured project.

An authorised user must be able to:

```text
enter configured project
  ->
load persisted Discussion
  ->
post
  ->
reload or leave
  ->
return and recover context
  ->
explicitly refresh
  ->
see another authorised user's committed message
```

Realtime and automatic push convergence are explicitly outside this slice.

## Governance Reconciliation

Milestone: M1 — Controlled Pilot
Scope Baseline Version: 1.0
Capability IDs Advanced: C08, C08.1, C08.2, C08.3 (partial advancement),
C08.4, INIT-AC-03
Foundational Requirements Affected: F02, F03, F05, F07, F08, F09, F12, F15,
F16
Regression Protected: C08.4, C07 authorization behavior, C18/C19 existing
proposal and provenance behavior, and the VS-001 Discussion POST/event flow
Dependencies: M0 governance closure; closed VS-001 Discussion POST and event
flow; closed VS-002 ProjectAuthorisationService integration
Original Commitments Incorporated: None
New Post-Initiation Capabilities: 0
Baseline Requirements Deferred Within M0–M3: C08.3 automatic/realtime
convergence; C09 broader Discussion collaboration; C10 editing; C20
notifications; C22 search; the other product and M3 capabilities remain at
their governed milestones.
Reason: M1 requires durable shared context and deterministic manual refresh.
Realtime convergence and broader Discussion functionality remain due later
without being removed or moved beyond M3.

F02 is affected because this slice adds a published query/service/API
contract. F04 and F10 remain regression-protected, but are not materially
changed by the frozen design: no schema migration, event producer, or outbox
boundary change is expected.

## Problem statement

Before VS003, VS-001 proved authenticated Discussion creation, immutable version
persistence, and asynchronous `MessageCreated.v1` processing. The browser then
kept message state only for the current session, and the API exposed no
project-message list/read route.

Without a read/return path, a second user or returning user cannot recover the
shared project context required by M1.

## Scope

### API and module contract

- Add an authenticated project-scoped Discussion list/read contract through the
  Discussion module.
- Add `GET /api/v1/projects/:projectId/messages` or the equivalent published
  route agreed by the existing API conventions.
- Enforce project access through the canonical
  `ProjectAuthorisationService` and permission codes.
- Return current committed message versions in deterministic order.
- Return the author identity/type and committed timestamp needed for pilot
  collaboration.
- Preserve the standard response, request-ID, and correlation-ID envelope.
- Define empty, loading, failure, and retry behavior at the boundary.

### Browser behavior

- Load messages when entering the configured project workspace.
- Restore messages after browser reload and after leaving and returning.
- Provide explicit manual refresh.
- Provide a visible retry path for read failure.
- Show sufficient author and time information to distinguish messages from
  multiple users.
- Keep posting through the existing authenticated API command.

### Security and integrity

- Re-evaluate authorization at the server read boundary.
- Preserve project isolation and concealment/denial semantics.
- Allow authorised read-only Observer and Auditor scenarios to read according
  to their permissions, while preserving their inability to post.
- Keep browser business data API-only; no direct browser business-table access.
- Preserve RLS and service-role boundaries as defence in depth.
- Preserve VS-001 and VS-002 frozen behavior.

## Explicit non-scope

- realtime subscriptions;
- automatic push convergence;
- message editing or deletion;
- full threads/reply UX;
- mentions;
- reactions;
- attachments and file management;
- read receipts;
- search;
- notifications;
- Topics, Decisions, Blockers, or Milestones;
- project discovery;
- self-service project creation;
- external onboarding;
- frontend/API hosting;
- worker scheduling;
- backup/recovery; and
- any VS-001 or VS-002 frozen-contract amendment.

These exclusions control this slice only. They do not remove or move any
baseline commitment beyond its existing M2/M3 path.

## Expected implementation surfaces

- Discussion repository read contract and Supabase adapter;
- Discussion service read method;
- authenticated API route and API contract documentation;
- browser Discussion query/hook/panel state;
- API, repository, authorization, and browser tests; and
- Discussion module documentation if the published contract changes.

No migration is expected because the persisted `messages` and
`message_versions` structures already exist. If implementation inspection
demonstrates that a structural or index migration is actually required, stop
and report the finding for governance review rather than silently adding a
migration to VS003.

## Security boundaries

The implementation must preserve:

- `ProjectAuthorisationService` as the project authorization boundary;
- membership-based project isolation;
- permission-code, server-authoritative access decisions;
- Observer/Auditor read-only behavior;
- API-only browser access to business data;
- no service-role key in the browser;
- RLS defense in depth;
- module ownership and no direct cross-module persistence access; and
- all frozen VS-001/VS-002 authentication, authorization, event, provenance,
  and audit behavior.

Reading a message does not authorize a user to post, edit, review proposals,
or mutate Tasks. Those permissions remain independently enforced.

## Approved acceptance boundary

1. An authenticated authorised Member, Owner, Manager, or Sponsor can load the
   configured project’s persisted Discussion.
2. An authorised Observer or Auditor can read where the existing permission
   model grants Discussion read access, but cannot post through the existing
   `message.create` boundary.
3. A non-member cannot read another project’s messages; existing concealment or
   denial semantics remain intact.
4. Messages are project-isolated and returned in deterministic order.
5. The response identifies the current committed version, author/type, and
   committed time required for pilot use.
6. Entering the workspace performs an initial read.
7. Browser reload restores persisted messages.
8. Leaving and returning restores the same persisted context.
9. Explicit refresh causes a second authorised browser session to see a first
   user’s committed message.
10. Empty, loading, error, and retry states are visible and usable.
11. Existing POST behavior remains intact, including its validation,
    authorization, immutable version, and atomic event behavior.
12. Existing Team Agent and Audit processing remains intact.
13. No browser business-table access is introduced.
14. C08.1 and C08.2 are eligible for closure only if all implementation and
    runtime evidence passes.
15. C08.3 receives only the deterministic manual-refresh/shared-visibility
    advancement in this slice; realtime/automatic convergence remains due M2.
16. C08.4 remains delivered and regression protected.
17. INIT-AC-03 is eligible for closure only if its complete acceptance
    evidence passes; the contract itself does not close it.

## Definition of Done

- API, service, repository, response, and authorization contracts are
  documented.
- Automated tests cover authenticated read, project isolation, permission
  denial, deterministic ordering, current-version selection, empty results,
  failures, and POST/event regression.
- Browser tests cover initial load, explicit refresh, browser reload, leave and
  return, empty state, failure, retry, and read-only role behavior.
- Two authenticated users complete a runtime smoke in separate sessions:
  user one posts a message and user two sees it after explicit refresh.
- The same persisted message is recovered after reload or later return.
- Team Agent proposal processing, Task materialisation, and Audit behavior
  remain passing and unchanged in their frozen boundaries.
- No service-role credential or direct business-table access reaches the
  browser.
- No VS-001 or VS-002 frozen requirement is changed.
- The traceability register records the approved sequencing reconciliation and
  evidence-based implementation closure status.
- Documentation and module ownership updates required by the published
  contract are complete.
- The closure reconciliation records:

```text
Original commitments removed: 0
Original commitments moved beyond M3: 0
Unmapped implementation changes: 0
```

## Implementation and closure evidence

Implementation checkpoints:

```text
89babaf  persisted Discussion read repository
b69b2d0  canonical read authorization
90c5b58  GET /api/v1/projects/:projectId/messages
a8c2b39  persisted browser loading and race correctness
2ff9ea4  deterministic Refresh and Retry
e8e6f61  Auditor Discussion permission regression evidence
5efa43e  deterministic VS003 local runtime fixture
```

R03B/R03C commits `13a27f1` and `6e21b76` are retained as adjacent migration
hardening evidence only; they are not attributed to C08 closure.

Automated evidence:

- DiscussionPanel: 47 tests; full web suite: 58/58.
- API suite: 404/404; API typecheck passes.
- Deterministic fixture: 75/75, with 5 persons/public users/active identities,
  5 memberships, 7 assignments, 14 events, and 7 correlations.
- Web lint: 0 warnings and 0 errors; CI build passes; `git diff --check`
  passes.

Stage 7A API/runtime evidence used the authenticated fixture actors and passed
authentication 5/5, User A POST 201 plus persisted GET, repeated-read
determinism, User B shared P1 visibility, P1/P2 isolation, Observer/Auditor
read access with 403 write denial, nonmember concealment, unauthenticated 401,
deterministic ordering, fresh return read, and the database read-only
cross-check. Unauthorized mutations, P1/P2 leakage, observed Security P0,
and Application P1 counts were all 0.

Stage 7B browser evidence was completed manually in the user's normal browser
after Codex initially had no browser surface. A local ignored
`apps/web/.env.development.local` project-ID change pointed the test setup at
VS003 P1; it remained outside Git and is not a product change. User A loaded,
reloaded, left/returned, and posted a message through the API. User B saw the
committed message in a separate browser context, refreshed repeatedly without
duplicates, and created a second message that User A saw and that survived
reload and return. The exact User B POST status was not retained and no 201 is
claimed for it. Observer and Auditor composers remained visible; their write
attempts were denied and no denied message persisted. Discussion business
traffic used `/api/v1`; direct Supabase business-table/RPC calls observed: 0
(authentication calls are excluded).

## Baseline closure reconciliation

| Requirement | Previous milestone/status | VS003 treatment | Residual gap |
|---|---|---|---|
| C08.1 — project rooms/messages | M2 / PARTIAL | DELIVERED: authenticated POST plus persisted project read and shared visibility. | Broader C08 remains partial. |
| C08.2 — persisted conversations reload after return | M1 / OUTSTANDING | DELIVERED: fresh GET reconstruction after reload and leave/return. | None within this child. |
| INIT-AC-03 — “Hold project discussions.” | M2 / PARTIAL | DELIVERED: posting, persisted shared visibility, reload/return, and protected role behavior evidenced. | C08.3 realtime behavior is separate. |
| C08.3 — committed messages including realtime/update behavior | M2 / OUTSTANDING | PARTIAL: deterministic manual Refresh, persisted shared visibility, fresh GET, and no duplicates. | Realtime/push-based automatic convergence remains due M2. |
| C08.4 — asynchronous AI/event hooks | M0 / DELIVERED | REGRESSION PROTECTED; not newly delivered by VS003. | Preserve VS-001 event flow. |

Affected foundations F02, F03, F05, F07, F08, F09, F12, F15, and F16 remain
`FOUNDATIONAL — ACTIVE` with VS003 evidence added. F04 and F10 are regression
only: VS003 introduced no migration or event producer. No affected foundation
is claimed fully delivered by this slice.

The VS003 non-scope remains explicit: no realtime subscriptions, automatic
convergence, editing, complete threading, mentions, reactions, attachments,
read receipts, search, notifications, topic/decision/blocker/milestone
expansion, project discovery/creation, external onboarding, hosting, worker
scheduling, or backup/restore.

```text
Original commitments removed: 0
Original commitments moved beyond M3: 0
Unmapped implementation changes: 0
Security P0: 0
Application P1: 0
```

## Closure recommendation

VS003 is ready for governance closure documentation review. The next action
after the VS003 closure commit is VS004 — Controlled Pilot Bootstrap and
Access. Under the single-stream rule, VS004 starts only after that closure
commit; realtime/automatic convergence remains scheduled under C08.3/M2.
